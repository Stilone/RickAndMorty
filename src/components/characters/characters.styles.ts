import styled from 'styled-components';

export const Content = styled.div`
  z-index: 1;
  margin-top: 40px;
  display: flex;
  flex-direction: column;
  align-items: center;
`;

export const RequestFailed = styled.span`
  margin-top: 60px;
`;